const {name,surname,showfuc} = require("./object_literals"); //importing only the objects in something object


console.log(name);
console.log(surname);
showfuc();

const nameTwo = require('./object_literals').name; //exporting just the name field in object literal
console.log(nameTwo);


const something = require('./object_literals'); //importing the whole object literal
something.showfuc();
